# บันทึกส่งต่องานล่าสุด

อัปเดต 4 กันยายน 2026 จากการอ่านไฟล์ใน workspace และประวัติคำขอผู้ใช้

## งานล่าสุดและจุดเริ่มต่อ

คำขอล่าสุด: ตรวจสถานะโปรเจกต์ แล้วแยกเซฟโหมดทดสอบออกจากเซฟผู้เล่น ไม่แตะ gameplay หรือ schema เซฟ

- `src/services/save.ts` แยกเป็น 2 key: `aquatic-adventure-save-v1` สำหรับผู้เล่น และ `aquatic-adventure-save-v1-devtest` สำหรับ dev server ตัดสินด้วย `import.meta.env.DEV` แทนการเช็ค port ทุก read/write ผ่าน `getSaveKey()`
- `adoptPlayerSaveForDevTest()` ใน `src/main.ts` คัดลอกเซฟผู้เล่นมาเป็นจุดตั้งต้นของ dev ครั้งเดียว ไม่เขียนทับ key ผู้เล่น
- `seedPrototypeTestData()` เปลี่ยนเงื่อนไขเป็น `isDevTestMode()` ผลพลอยได้คือ prod bundle tree-shake โค้ด seed ทิ้งทั้งก้อน
- เพิ่ม `vite.config.ts` แยก Phaser เป็น chunk (`index` 249 kB / `phaser` 1,208 kB) ลดการดาวน์โหลดซ้ำเมื่อ deploy ใหม่ ไม่ได้ลดขนาดโหลดครั้งแรก
- เพิ่ม `npm test` = `node --test tests/*.mjs` และ `src/vite-env.d.ts` สำหรับ type ของ `import.meta.env`

ตรวจจริงแล้ว: `npm run build` ผ่าน · `npm test` ผ่าน 14/14 · prod bundle มีเฉพาะ key ผู้เล่น ไม่มีสตริง `devtest` และไม่มี `testDataVersion` · บน `127.0.0.1:5173` ยืนยันตัวละครชื่อ “ทดสอบ” แล้วเข้า WorldScene ได้ HUD ขึ้นเลเวล 10 / 5000 เหรียญ ขณะที่ key ผู้เล่นเทียบสตริงแล้วไม่เปลี่ยนและยังไม่มี `character`

ล้างเซฟทดสอบได้ปลอดภัยด้วย console: `localStorage.removeItem("aquatic-adventure-save-v1-devtest")` แล้วรีโหลด จะได้ประสบการณ์เริ่มเกมจากศูนย์จริง

ต่อจากนั้นทำ HUD แผนที่ใหม่ตามภาพคอนเซปต์ที่ผู้ใช้ส่งมา แก้ที่ `src/ui/worldHud.ts` และ `src/scenes/WorldScene.ts` เท่านั้น ไม่แตะ gameplay

- ไอคอนเมนูเปลี่ยนจากเส้น 2.6px เป็นรูปทึบใน `drawMenuIcon()` และเพิ่มชนิด `craft`
- เพิ่ม `createHudDiscButton()` เป็นปุ่มกลมมาตรฐาน จานฟ้า ขอบครีม เงานุ่ม ป้ายชื่อบนแถบครีม ใช้ร่วมกันทั้ง 6 ปุ่มเมนูและปุ่มสร้างของ; `WorldScene.createCraftButton()` เลิกวาดปุ่มเองและเรียกตัวนี้แทน จึงไม่มีปุ่มสไตล์เก่าค้าง
- แถบบน: ป้ายสถานที่และชิปสกุลเงินเป็นพิลกรมท่า `CHIP_NAVY` มีหมุดแผนที่ ไอคอนกลม ตัวเลขขาว; ชิปเหรียญมีปุ่ม `+` ไป `ShopScene`
- แผงผู้เล่น: แถบ XP เป็นเขียว เพิ่มใบไม้หน้าชื่อและดาวทองท้ายแถบ
- ย่อป้ายเมนูให้สั้นเท่ากัน: กระเป๋า ตัวละคร สมุดปลา ตู้ปลา ประลอง ตั้งค่า สร้างของ

ตรวจแล้ว: build ผ่าน, `npm test` ผ่าน 14/14, ดูบนเกมจริงที่ 5173 ทั้งแถบบนและแถวไอคอน

ยังไม่ได้ทำและผู้ใช้อาจถามถึง: ภาพคอนเซปต์มีแถวพยากรณ์อากาศ 4 ช่องในป้ายสถานที่ แต่ระบบมีเฉพาะอากาศปัจจุบัน ยังไม่มีข้อมูลพยากรณ์ จึงคงเป็นนาฬิกา+ช่วงเวลาไว้; ชิปแต้มอนุรักษ์ไม่มีปุ่ม `+` เพราะยังไม่มีปลายทางให้เติมแต้ม; ไอคอนยังเป็นเวกเตอร์ ไม่ใช่ภาพวาดแบบในคอนเซปต์ ถ้าต้องการหน้าตาแบบนั้นจริงต้องทำไฟล์ PNG แล้ววางใน `public/assets/ui/` เหมือน `quest-board-v1.png`

รอบถัดมาแก้เรื่อง “ใส่ชุดแล้วดูเหมือนแปะไว้” ที่ `src/ui/avatarRenderer.ts`, `src/types/avatar.ts`, `src/data/avatarData.ts`

สาเหตุที่วัดได้จริง ไม่ใช่การเดา (วัดขนาดและกรอบวัตถุของ PNG ผ่าน canvas ในเบราว์เซอร์):

1. หมวกถูกยืด: ไฟล์ 1536x1024 (อัตราส่วน 1.5) แต่ placement เดิมสั่ง `width .72, height .23` บนกรอบ 1024x1536 ทำให้กว้างเกินจริงราว 39%
2. `placement` ที่ไม่ระบุ `y` ใช้ค่า `originY` เป็นค่าเริ่มต้น ซึ่งเท่ากับ 1 ทำให้ชิ้นถูกวางกึ่งกลางที่ขอบล่างของกรอบ เป็นบั๊กแฝงของค่าเริ่มต้น
3. เลเยอร์เครื่องแต่งกายไม่ได้รับ `tint` เดียวกับตัวละครฐาน สีจึงไม่เข้าโทนกัน

แก้แล้ว: `resolveLayerSize()` รองรับ 3 โหมด — ไม่ระบุ placement = ทับกรอบ 1:1, ระบุเฉพาะ `width` = คงสัดส่วนจริงของไฟล์, ระบุทั้งคู่ = ยืดตามสั่ง; ค่าเริ่มต้นของ x/y เป็น .5; เลเยอร์รับ tint เดียวกับฐาน; ปรับ placement ของหมวกเป็น `{x:.5, y:.06, width:.6}` และเสื้อเป็น `{x:.5, y:.534, width:.72}`

ตรวจแล้ว: build ผ่าน, `npm test` ผ่าน 15/15 (เพิ่มเทสต์ว่าเลเยอร์ต้องคงสัดส่วนจริงเสมอ), ดูบนเกมจริงทั้งหน้าโลกและหน้า “ตัวละคร”

ข้อจำกัดที่เหลือ แก้ด้วยโค้ดไม่ได้ ต้องวาดภาพใหม่:

- `rain-coat-layer-v2.png` เป็นภาพชุดเปล่าแบบภาพสินค้า ลอยกลางผืน ไม่มีตัวคนอยู่ข้างใน ฮู้ดจึงเป็นโพรงว่างและแขนเสื้อไม่ตรงกับแขนตัวละคร
- `straw-hat-v1.png` วาดจากมุมต่ำ เห็นใต้ปีกหมวก ขณะที่ตัวละครมองจากด้านหน้า มุมมองจึงไม่ตรงกัน
- ทั้งสองชิ้นใช้ไฟล์เดียวกันทั้งท่า idle และ walk เสื้อผ้าจึงนิ่งสนิทขณะตัวละครขยับ
- ทางแก้จริงคือวาดชิ้นใหม่บนผืน 1024x1536 เดียวกับตัวละคร แยกไฟล์ต่อท่า ตามที่ `docs/AVATAR_SYSTEM.md` กำหนดไว้อยู่แล้ว

ต่อมาเพิ่มระบบจุดยึด (anchor) เพราะฐานตัวละคร 8 แบบมีหัวคนละตำแหน่ง แต่ของสวมหัวใช้ค่าคงที่ค่าเดียว

- `AvatarTextureAsset` มีฟิลด์ `head: { cx, top, width }` วัดจากไฟล์ภาพจริงด้วย canvas ครบ 16 ค่า (8 ฐาน x 2 ท่า) อยู่ใน `src/data/avatarData.ts`
- `placement` รองรับโหมด `{ anchor: "head", widthScale, offsetY }` ชิ้นจะเกาะหัวจริงของฐานนั้นท่านั้น
- `avatarRenderer` คำนวณตำแหน่งและขนาดใหม่ทุกครั้งที่เปลี่ยนท่า ไม่ใช่คำนวณครั้งเดียวตอนสร้าง
- หมวกฟางเปลี่ยนเป็น `{ anchor: "head", widthScale: 1.31, offsetY: .048 }` ค่าเดียวใช้ได้ถูกต้องทั้ง 8 ฐาน

ตัวเลขที่วัดได้ ใช้อ้างอิงเวลาเพิ่มของใหม่:

- หัวต่างกันมาก: ขอบบน .003-.048, ความกว้าง .375-.500 (ต่างกัน 33%), กึ่งกลาง .449-.498
- ลำตัวเกือบเหมือนกันทุกฐาน: ไหล่ .351-.416, เอว .459-.487, เข่า .298-.314, พื้นเท้า .966-.976
- แปลว่า **ชุด/รองเท้า/ถุงมือ วาดชุดเดียวใช้ได้ทั้ง 8 ฐาน** ส่วนของสวมหัวต้องพึ่ง anchor

ตรวจแล้ว: build ผ่าน, `npm test` ผ่าน 16/16 (เพิ่มเทสต์ว่าหมวกต้องกว้าง/สูงต่างกันตามฐาน เยื้องตามหัว และขยับเมื่อเปลี่ยนท่า)

ต่อมาขยายระบบแต่งตัวให้รองรับของจำนวนมากต่อช่อง (ผู้ใช้ระบุว่าจะมีอย่างละ ~20 แบบ) ทำเฉพาะระบบ ยังไม่ทำกราฟิก

- `FashionSlot` เพิ่ม `gloves` ครบเป็น hat/outfit/shoes/gloves; `FASHION_SLOTS` ย้ายมาใช้ชนิดที่ผูกกับ `FashionSlot` จริง
- `FashionDefinition` เพิ่ม `previewTexture` ไม่บังคับ ตู้เสื้อผ้าจึงโหลดภาพตัวอย่างตามข้อมูล เลิก hardcode `straw-hat`
- เพิ่ม `fashionItemsInSlot()` ใน `src/data/fashionData.ts`
- `WardrobeScene` เขียนใหม่จาก "หนึ่งแผงต่อช่อง" เป็น "แท็บช่อง + ตารางของ 3x3 + แบ่งหน้า" เดิม `drawSlot()` ใช้ `.find()` จึงแสดงได้ช่องละ 1 ชิ้นเท่านั้น ถ้าใส่หมวก 20 แบบจะเห็นแค่แบบแรก
- ลบโค้ดตายที่วาดอิโมจิรองเท้าทับตัวละคร (`equipped.shoes` เป็นจริงไม่ได้เพราะยังไม่มีไอเทมช่องนี้)

เพิ่มการเพิ่มของใหม่ตอนนี้ = 1 รายการใน `fashionData.ts` + 1 รายการใน `shopData.ts` + ภาพใน `avatarData.ts` ตู้เสื้อผ้าจัดหน้าเองไม่ต้องแก้ scene

ตรวจแล้ว: build ผ่าน, `npm test` ผ่าน 24/24 (เพิ่ม `tests/fashion.test.mjs` 8 เคส ครอบคลุมของที่ถูกถอดออกจากแค็ตตาล็อก ของที่ขายไปแล้ว และ id ที่ไม่มีจริง ต้องไม่ทำให้เซฟพัง), เปิดตู้เสื้อผ้าจริงแล้วสลับแท็บ หมวก/ชุด/รองเท้า/ถุงมือ ได้ ช่องว่างขึ้นข้อความถูกต้อง

ยังไม่ได้ทำ ตั้งใจเว้นไว้: กฎการซ่อน (ใส่หมวกแล้วซ่อนผม) ยังทำไม่ได้จริงเพราะภาพฐานเป็นภาพรวมชิ้นเดียว ไม่มีเลเยอร์ผมแยก ต้องรอภาพฐานที่แยกชิ้นก่อน จึงไม่สร้าง abstraction ทิ้งไว้ลอยๆ

ต่อมาแก้บั๊กคลาสเดียวกันในร้านค้า ซึ่งจะบล็อกแผนของ 20 ชิ้นต่อช่องโดยตรง

- `ShopScene.renderCatalog()` เดิม `items.forEach` วางการ์ดที่ `y = 273 + row * 158` การ์ดสูง 138 แต่พื้นที่ใช้ได้ถึง y=626 เท่านั้น (ขอบล่างแผงรายละเอียด) จึงเห็นจริงแค่ 2 แถว = **4 ชิ้นต่อหมวด** ชิ้นที่ 5 เป็นต้นไปวาดหลุดนอกแผง มองไม่เห็นและกดซื้อไม่ได้
- เพิ่มการแบ่งหน้า 2x2 ต่อหน้า พร้อมปุ่มก่อนหน้า/ถัดไป และตัวนับหน้าในหัวข้อ
- ดึงตรรกะแบ่งหน้าออกเป็น `src/ui/paging.ts` (`pageSlice`) ใช้ร่วมกับตู้เสื้อผ้า เลิกเขียนซ้ำสองที่ และ `page` ที่เกินช่วงถูกบีบกลับ ไม่ปล่อยให้เจอหน้าว่าง

ตรวจแล้ว: build ผ่าน, `npm test` ผ่าน 29/29 (เพิ่ม `tests/paging.test.mjs` 5 เคส ข้อสำคัญคือ "ทุกชิ้นต้องอยู่บนหน้าใดหน้าหนึ่งพอดีครั้งเดียว" ทดสอบที่ 0/1/4/5/9/20/21/80 ชิ้น), เปิดร้านค้าจริงผ่านปุ่ม `+` บนชิปเหรียญ เลย์เอาต์ปกติ

หมายเหตุ: ทุกหมวดในร้านมี 2 ชิ้นพอดี ตัวแบ่งหน้าจึงยังไม่ปรากฏบนจอจริง ความถูกต้องยืนยันด้วยเทสต์ ไม่ใช่ภาพหน้าจอ

ยังไม่ได้ทำ ตั้งใจเว้นไว้: กฎการซ่อน (ใส่หมวกแล้วซ่อนผม) ยังทำไม่ได้จริงเพราะภาพฐานเป็นภาพรวมชิ้นเดียว

ต่อมาทำ validate เซฟตอนอ่านใน `src/services/save.ts`

- เพิ่ม `sanitizeSaveData()` ตรวจทีละฟิลด์ `readSaveData()` เรียกใช้ก่อนคืนค่า
- หลักการ: **ซ่อมทีละฟิลด์ ไม่ทิ้งทั้งก้อน** ฟิลด์ที่ผิดรูปหายไปเฉพาะฟิลด์นั้น ความคืบหน้าที่เหลืออยู่ครบ
- ตรวจลึก: ตัวเลขต้องเป็น finite number (`"5000"` และ `NaN` ถูกตัดทิ้ง), record ของตัวเลข, อาร์เรย์ของสตริง, `records` ต้องเป็นคู่ `[string, number]`, `weather` ต้องเป็นค่าที่มีจริง, `inventory.fish` เติมฟิลด์ที่ขาดเป็น 0 กัน NaN ลาม
- ตรวจแค่รูปร่าง (เป็น object ไหม): `aquarium`, `breeding`, `battle`, `dailyQuests`, `market`, `character`, `equippedFashion`, `quests` เพราะ service ของแต่ละระบบตรวจเนื้อในเองอยู่แล้ว จงใจไม่ไปตีความ schema ที่ยังไม่ได้ศึกษาครบ
- **ฟิลด์ที่ไม่รู้จักถูกส่งต่อตามเดิม** สำคัญมาก เพราะ `writeSaveData()` ทำ `{...readSaveData(), ...update}` ถ้าคัดเฉพาะฟิลด์ที่รู้จัก เซฟจากเวอร์ชันใหม่กว่าจะถูกลบทิ้งตอนเขียนกลับ

ตรวจแล้ว: build ผ่าน, `npm test` ผ่าน 38/38 (เพิ่ม `tests/save.test.mjs` 9 เคส), เปิดเกมจริงเซฟเดิมอ่านได้ปกติ ไม่มี console error

แก้เทสต์เดิมหนึ่งจุด: `tests/character.test.mjs` เคย fixture กองปลาเป็น `{count: 3}` แล้วคาดว่าจะได้คืนเท่าเดิม ตอนนี้ถูกเติมเป็น 0 ครบทุกฟิลด์ตามที่ตั้งใจ จำนวนที่มีอยู่จริงไม่เปลี่ยน

ต่อมาเริ่มระบบสมุดบันทึกภาคสนาม ตามข้อเสนอใน `docs/JOURNAL_SYSTEM.md` — **ทำแล้วเฉพาะขั้น 1** ยังไม่มี UI

- ไฟล์ใหม่: `src/types/journal.ts`, `src/data/journalData.ts`, `src/services/journal.ts`
- เพิ่ม `speciesLog?: SpeciesLog` ใน `SaveData` พร้อม validator ใน `sanitizeSaveData()` ที่ตัดช่องซ้ำและช่องที่ไม่ใช่สตริงทิ้ง
- `migrateSpeciesLog()` เรียกใน `src/main.ts` ตอนเปิดเกม ให้เซฟเก่าที่มี `discoveredSpecies` ได้ `catchCount: 1` แต่ **ช่องว่างเปล่า** จงใจไม่เดาย้อนหลัง
- จำนวนช่องคำนวณจาก `biomes.length x 4 x 3` ชนิดที่ไม่มีข้อมูลนิเวศ (ปลากระโทงดาบ) ถือว่าพบได้ทุกแหล่ง เพราะ `rollCatch` ไม่กรองแหล่งน้ำให้
- คำสรุปนิสัยสร้างจากค่าจริงใน `FISH_ENVIRONMENT_WEIGHTS` ไม่ได้เขียนข้อความตายตัวไว้ ปรับสมดุลแล้วข้อความขยับตามเอง

ตรวจแล้ว: build ผ่าน, `npm test` ผ่าน 51/51 (เพิ่ม `tests/journal.test.mjs` 13 เคส), เปิดเกมจริงแล้ว migration ทำงานถูก เซฟ dev ได้ `speciesLog` 8 ชนิด ช่องว่าง เงินไม่เปลี่ยน ไม่มี console error

**ขั้น 2 ทำแล้ว**: `FishingScene.resolveCatch` ใน `src/main.ts` เรียก `recordCatch()` ในสาขาที่ไม่ใช่ขยะ ต่อจาก `this.collectionCount += 1` ส่ง `this.biome`, `getTimePeriod(this.worldState.minutes)`, `this.worldState.weather` บันทึกทุกครั้งที่จับได้ ไม่ว่าจะเก็บ ขาย หรือปล่อย

ตรวจแล้ว: build ผ่าน, `npm test` ผ่าน 51/51

**สิ่งที่ยังตรวจไม่ครบ ต้องอ่านก่อนทำต่อ**

- ตกปลาจริงในเกมหลายรอบแล้ว **ยังไม่เคยจับปลาสำเร็จสักตัว** ได้แต่ขยะกับหลุด จึงยังไม่ได้เห็นกับตาว่าจับปลาแล้วช่องถูกบันทึก
- ที่ยืนยันได้จริง: เรียก `recordCatch()` ตรงๆ ผ่าน dev server ในเบราว์เซอร์ ทำงานถูกต้อง (เพิ่มช่อง, `catchCount` 1→2, `firstSeenDay` ถูกตั้ง, progress 1/12) และ **จับขยะไม่แตะสมุดเลย** ซึ่งถูกต้อง ทดสอบซ้ำ 6-7 ครั้ง
- มีจุดที่ยังอธิบายไม่ได้: `collectionCount` ขยับ 80 → 81 ระหว่างรอบทดสอบ ทั้งที่ `collectionCount += 1` มีที่เดียวคือ `src/main.ts:1246` ซึ่งอยู่ติดกับ `recordCatch()` ถ้าปลาตัวนั้นถูกจับหลังโค้ดใหม่โหลดแล้ว สมุดต้องมีช่อง แต่ไม่มี — **ยังสรุปไม่ได้ว่าเป็นบั๊กหรือเป็นค่าที่ค้างมาก่อนแก้โค้ด**
- วิธีตรวจให้จบใน 10 วินาที: ตกปลาให้ได้ 1 ตัว (ไม่ใช่ขยะ) แล้วรันใน console
  `JSON.parse(localStorage.getItem("aquatic-adventure-save-v1-devtest")).speciesLog`
  ถ้าชนิดนั้นมี `cells` เพิ่มขึ้นหนึ่งช่อง แปลว่าขั้น 2 ถูกต้อง ถ้าไม่เพิ่ม ให้ไล่ดู `resolveCatch` ก่อนทำขั้น 3

ขั้นถัดไปของระบบนี้: ขั้น 3 ตารางนิเวศในหน้าสารานุกรม (`PlayerMenuScene.drawDex`) ซึ่งจะทำให้ตรวจขั้น 2 ได้ด้วยตาทันทีโดยไม่ต้องเปิด console

งานถัดไปที่เสนอ: ยก `FishingScene` ออกจาก `src/main.ts` (1,631 บรรทัด) และตัด `HubScene` / `DexScene` / `EquipmentScene` ที่ยังลงทะเบียนอยู่แต่ไม่มีทางเข้าถึงจากเส้นทางเล่นจริง ทำ validator ต่อฟิลด์แบบเดียวกับที่ `src/services/character.ts` ทำอยู่

งานก่อนหน้าในโค้ดที่ตรวจพบ: ระบบตัวละครแยกชิ้น, ผูก preset กับฐานหน้าที่คงเดิมและบันทึก `baseVariant`, หน้าตู้เสื้อผ้ามีตัวอย่างยืน/เดิน พร้อมไฟล์ทดสอบตัวละครและตัวอย่างแอนิเมชัน

หากผู้ใช้บอก “ไปต่อ” โดยไม่มีรายละเอียด: เริ่มจากตรวจหน้าตู้เสื้อผ้าบนเกมจริง สลับชุด/หมวกและยืน/เดิน ตรวจการซ้อนเลเยอร์และรูปลักษณ์ในหน้าโลกก่อน แล้วแก้เฉพาะปัญหาที่พบ ไม่ต้องเริ่มสร้างโปรเจกต์ใหม่

## วิธีรันและตรวจ

โฟลเดอร์: `C:\Users\admin\Documents\ChatGPT\fishing`

```powershell
npm run dev
# เปิด URL ที่ Vite แสดง ปกติ http://127.0.0.1:5173/
npm run build
npm test
```

- ตรวจเซิร์ฟเวอร์เดิมก่อนเปิดเพิ่ม หากพอร์ตถูกใช้ Vite อาจเลือกพอร์ตใหม่ อย่าชี้ผู้ใช้ไปหน้าค้างของอีกเซิร์ฟเวอร์
- เกมใช้ Phaser 3.90 / TypeScript / Vite, 1280×720 แบบ FIT, ฟอนต์ Mitr
- `npm test` = `node --test tests/*.mjs` ไฟล์ทดสอบใช้ esbuild ที่มีอยู่ใน dependency tree และ mock `localStorage` เอง ไม่แตะเซฟจริง
- รอบล่าสุดรัน build และ tests ผ่าน และเปิดหน้าเกมจริงแล้ว แต่ยังไม่ได้ไล่ตรวจทุกฉาก

## สถานะระบบที่พบในโค้ด

“มีโค้ด” หมายถึงพบ implementation ไม่ได้แปลว่าตรวจคุณภาพหรือทดสอบครบแล้ว

| ระบบ | จุดแก้หลัก | สถานะ/ขอบเขตที่ทราบ |
|---|---|---|
| เริ่มเกม/เลือกตัวละคร | `src/scenes/CharacterSetupScene.ts`, `src/services/character.ts`, `src/data/characterData.ts` | มี 20 preset, ยืนยันชื่อและตัวละคร; ฐานภาพชาย 4 หญิง 4 ใช้ซ้ำใน preset ยังไม่ใช่หน้าตาไม่ซ้ำ 20 แบบ |
| แต่งตัวและ preview | `src/scenes/WardrobeScene.ts`, `src/ui/avatarRenderer.ts`, `src/ui/avatarPreview.ts`, `src/data/avatarData.ts` | มีเลเยอร์และท่ายืน/เดิน; อ่าน AVATAR_SYSTEM.md ก่อนเพิ่มชุด |
| หมู่บ้านชายฝั่ง/แม่น้ำ | `src/scenes/WorldScene.ts`, `src/scenes/RiverScene.ts`, `src/data/worldNavigation.ts` | พบ 2 ฉากสำรวจและข้อมูลเดินชน; หลายแผนที่อื่นยังเป็นเป้าหมาย |
| น้ำและอากาศ | `src/ui/waterEffects.ts`, `src/ui/weatherEffects.ts`, `src/services/worldTime.ts` | มี implementation ต้องตรวจภาพเคลื่อนไหวจริง |
| ตกปลา | `src/main.ts` คลาส `FishingScene`, `src/services/fishSelection.ts` | state ready/casting/waiting/hooking/fighting/caught/escaped อยู่ใน main.ts จริง |
| คันเบ็ด/เหยื่อ/ยา | `src/services/equipment.ts`, `src/services/bait.ts`, `src/services/potions.ts`, `src/data/rodArt.ts`, `src/ui/baitSelector.ts` | มีข้อจำกัดคันเบ็ด อัปเกรด สต็อกและการใช้ของ |
| กระเป๋า/สมุด/การ์ดเรียนรู้ | `src/scenes/PlayerMenuScene.ts`, `src/services/inventory.ts`, `src/data/speciesEducation.ts`, `src/data/fishArt.ts` | มีการ์ดพลิก ภาพและข้อเท็จจริง; ยังต้องตรวจความถูกต้อง/เครดิตรายชนิดเมื่อเพิ่มเนื้อหา |
| เควส/รายวัน/เลเวล | `src/scenes/QuestScene.ts`, `src/scenes/DailyQuestScene.ts`, `src/scenes/LevelRewardScene.ts`, services ชื่อเดียวกับระบบ | มีเควสเริ่มต้น รายวัน และรับรางวัล; ไม่ใช่แคมเปญเต็ม |
| ร้าน/คราฟต์ | `src/scenes/ShopScene.ts`, `src/scenes/CraftScene.ts`, `src/services/crafting.ts` | มีร้านและสูตรสร้างของ |
| ตลาด | `src/scenes/MarketScene.ts`, `src/services/market.ts` | ขายปลา คำนวณราคาและเก็บประวัติในเครื่อง ไม่ใช่เทรดออนไลน์ |
| ตู้ปลา | `src/scenes/AquariumScene.ts`, `src/services/aquarium.ts`, `src/data/aquariumData.ts` | ใส่/นำออก ให้อาหาร ล้างและตกแต่ง; ความจุเริ่ม 1 เพิ่มทุก 2 เลเวล สูงสุด 6 ตาม config ปัจจุบัน |
| เพาะพันธุ์ | `src/scenes/BreedingScene.ts`, `src/services/breeding.ts` | มีเริ่ม/ยกเลิก/รับลูกและรางวัลความก้าวหน้า |
| ต่อสู้รอง | `src/scenes/BattleScene.ts`, `src/services/battle.ts` | มีผลแพ้ชนะและ mastery รายชนิด ไม่ใช่ PvP ออนไลน์ |
| บันทึก | `src/services/save.ts` | localStorage ใน browser ไม่มีเซฟบัญชี/คลาวด์ในระบบนี้ |

ข้อมูลปรับสมดุลและแค็ตตาล็อกอยู่ `src/data/`; ชนิดข้อมูลอยู่ `src/types/`; ภาพอยู่ `public/` ให้ตาม path จาก data/preload ไม่เดาชื่อไฟล์

## ข้อควรระวังที่ผู้รับช่วงต้องรู้

1. เซฟผู้เล่นใช้ key `aquatic-adventure-save-v1` ห้ามเปลี่ยนหรือล้าง; dev ใช้ `aquatic-adventure-save-v1-devtest` เซฟยังผูกกับ browser และ origin อยู่ `localhost` กับ `127.0.0.1` ยังเห็นคนละชุด
2. แก้แล้ว: `seedPrototypeTestData()` เขียนลง key `-devtest` เท่านั้น เมื่อรัน dev และ `testDataVersion < 1` เซฟผู้เล่นไม่ถูกแตะอีก `npm run preview` และ build จริงใช้ key ผู้เล่นตามปกติ
3. เซฟ dev เป็นของทิ้งได้ ลบ key `-devtest` เพื่อทดสอบประสบการณ์เริ่มจากศูนย์ ห้ามเพิ่ม `TEST_DATA_VERSION` เพื่อเติมของซ้ำโดยไม่ตรวจผล
4. `readSaveData()` ตอนนี้ parse ผิดแล้วคืน `{}` และยังไม่ validate schema ทั้งก้อน เป็นข้อจำกัดที่พบ ไม่ใช่ระบบกู้เซฟสมบูรณ์; ยังไม่ได้แก้ในรอบนี้
5. แม้แนวทางต้องการแยกฉาก แต่ `FishingScene`, `EquipmentScene`, `HubScene`, `DexScene` ยังอยู่ใน `src/main.ts`; อย่าเข้าใจว่าไฟล์ฉากตกปลาหาย และไม่ควรย้ายทั้งหมดระหว่างแก้งานเล็ก
6. เริ่มเกมจริงจาก `CharacterSetupScene` ตาม scene registration แล้วไป `WorldScene`; การมี HubScene เก่าไม่ใช่เหตุให้กลับไปทำหน้าหลักแบบเมนูซ้ำ
7. วันที่ตรวจ `git status --short` แสดงไฟล์โปรเจกต์หลักเป็น untracked รวม src/docs/public จึงไม่มี baseline diff ที่เชื่อถือได้สำหรับไฟล์เหล่านั้น ห้าม clean/reset/ลบสิ่งที่ไม่รู้จัก และอย่า claim ว่ามี commit สำรองแล้ว
8. `dist/`, `node_modules/`, `tmp/`, `.codex-remote-attachments/` อยู่ในเครื่อง อย่ายกขึ้นมาเป็น source หลักหรือรวมเป็นงานส่งโดยไม่จำเป็น
9. ปัญหาในแชตเก่า เช่น เดินผ่านรั้ว การ์ดล้น หมวกแปะลอย เป็นรายการตรวจ regression ไม่ใช่หลักฐานว่าบั๊กยังอยู่ทุกข้อ

## การอัปเดตบันทึกเมื่อจบงานถัดไป

แก้บันทึกนี้ให้สั้นและตรงปัจจุบัน: วันที่, คำขอที่เพิ่งทำ, ไฟล์สำคัญที่แก้, สิ่งที่ตรวจจริงพร้อมผล, ข้อจำกัด/งานค้าง, งานถัดไปหนึ่งชิ้น และ path ภาพหน้าจอถ้ามี อย่าบันทึกข้อมูลลับหรือข้อมูลเซฟผู้เล่นทั้งก้อน

เปลี่ยน GAME_PLAN.md เฉพาะเมื่อข้อกำหนดหรือแผนเปลี่ยน; ไม่ต้องคัดลอกแชตทั้งหมดลงไฟล์
